server {
    listen 80;
    server_name vendornest.in www.vendornest.in;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name vendornest.in www.vendornest.in;

    ssl_certificate /etc/letsencrypt/live/vendornest.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vendornest.in/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    client_max_body_size 20M;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    location /static/ {
        alias /home/ubuntu/vendornest/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}
